> **ONTOLOGY**: All workflows MUST reference \ for canonical term definitions.

> **நூல்**: **WHY** , External signals dictate strategy. | **WHAT TYPE** ,  INTELLIGENCE CAPTURE | **HOW** ,  Interrogate → Scan & Abstract → Persist

# /research , Formalize External Intelligence

> *"Read everything, but steal deliberately."*

RESEARCH uses the Level 1 STEAL protocol to convert raw external noise (URLs, topics) into refined, actionable signals tailored to your specific profile.

---

## Usage

```
/research "[Topic or URL]"
```

## Step 1: Pre-Reasoning Governance (PASS) -> HUMAN APPROVAL REQUIRED

Before starting, restate the user's intent back to them using the PASS framework. Apply these permanent directives to their specific query:

- **Purpose**: Extract high-leverage intelligence from external noise.
- **Anti-Purpose**: Do NOT output generic, encyclopedic summaries. We only extract what matters to the professional's baseline identity.
- **Success-Signal**: We extract exactly 3 actionable signals; output is marked via the STEAL framework.
- **Synthesis**: Transforms external abstraction into an internal intelligence ledger (`research/[topic].md`).

Present the calculated PASS table to the user and ask: *"Do you approve this execution intent?"*
**STOP AND WAIT FOR EXPLICIT HUMAN APPROVAL.**

## Step 2: Context Injection
*(Execute ONLY after PASS approval)*

Before executing the research, explicitly read the user's foundation so you are not reasoning blindly:
**Source**: `profile/core.md`

## Step 3: Synthesis Framework (STEAL Level 1)

Execute the intelligence capture using the **STEAL** framework:
- **S** - Scan *(Read the topic/URL)*
- **T** - Translate *(Filter the noise through the lens of their CORE profile)*
- **E** - Evaluate *(Determine what matters)*
- **A** - Abstract *(Generate exactly 3 actionable signals from the noise)*
- **L** - Leverage *(Explain exactly how these signals alter their current GROWTH strategy)*

## Step 4: Mandatory Persist

Save the resulting markdown artifact.

**File:** `research/YYYYMMDD-HHMMSS-topic.md` (Generate a timestamped filename)

Report to the user:
- "RESEARCH intelligence captured."
- "Saved to \`research/[filename].md\`."
