> **ONTOLOGY**: All workflows MUST reference \ for canonical term definitions.

> **நூல்**: **WHY** , Leverage lives in the human network. | **WHAT TYPE** ,  EXTRACTION (alliance mapping) | **HOW** ,  Read Priors → Interrogate → Synthesize → Persist

# /resonate , Map Harmonic Alliances

> *"Your power is bottlenecked by the rigidity of your grid."*

RESONATE maps the external nodes that scale your capability. It turns an abstract list of "connections" into a physical grid of leverage.

---

## Usage

```
/resonate
```

## Step 1: Pre-Reasoning Governance (PASS) -> HUMAN APPROVAL REQUIRED

Before starting, restate the user's intent back to them using the PASS framework. Apply these permanent directives to their specific query:

- **Purpose**: Map the external nodes that scale the user's capability in harmonic resonance.
- **Anti-Purpose**: Do NOT accept a simple list of friends or generic networking contacts. We only want load-bearing relationships.
- **Success-Signal**: We mathematically isolate energy flow; output is an Alliance Matrix via the NETWORK framework.
- **Synthesis**: Transforms abstract networks into physical leverage (`resonate/[network].md`) that will bottleneck or accelerate strategy.

Present the calculated PASS table to the user and ask: *"Do you approve this execution intent?"*
**STOP AND WAIT FOR EXPLICIT HUMAN APPROVAL.**

## Step 2: Context Injection
*(Execute ONLY after PASS approval)*

Read the user's foundation:
**Source**: `profile/core.md`
**Source**: `experience/*.md`

## Step 3: Interrogate

Ask the user: *"Who are the 4-5 load-bearing nodes in your professional network right now? Think across mentors, peers, influencees, and gatekeepers."*

## Step 4: Synthesis Framework (NETWORK)

Synthesize their answers into an Alliance Matrix using the **NETWORK** framework. For each person listed, extract:
- **N** - Node *(The person/entity)*
- **E** - Exchange *(The flow of value)*
- **T** - Trust *(The depth of the bond)*
- **W** - Worth *(The geopolitical leverage they provide)*
- **O** - Obligation *(What is explicitly or implicitly owed)*
- **R** - Reciprocity *(How you mutually scale)*
- **K** - Kinship *(Alignment of core philosophy)*

## Step 5: Mandatory Persist

Save the resulting markdown artifact.

**File:** `resonate/YYYYMMDD-HHMMSS-alliances.md`

Report to the user:
- "RESONATE grid generated."
- "Saved to \`resonate/[filename].md\`."
- "Next step: Run \`/strategise\`."
